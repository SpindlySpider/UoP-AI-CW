import numpy as np
from numpy._core.numerictypes import float64
from numpy._typing import NDArray

"""LEGACY MODULE REMOVED

Use `torch.nn.MSELoss` or `torch.nn.functional.mse_loss` for loss computation
when training PyTorch models.
"""

raise ImportError("nn.error_funcs has been removed. Use torch.nn.MSELoss instead.")
