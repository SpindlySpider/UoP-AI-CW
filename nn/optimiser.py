from nn.neural_network import Neural_network
import numpy as np
"""LEGACY MODULE REMOVED

This module implemented manual gradient descent for the NumPy-based network.
Use `torch.optim` (e.g. `torch.optim.SGD` or `torch.optim.Adam`) with the
PyTorch model instead.
"""

raise ImportError("nn.optimiser has been removed. Use torch.optim optimizers instead.")
    """
